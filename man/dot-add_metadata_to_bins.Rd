% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hextant_utils.R
\name{.add_metadata_to_bins}
\alias{.add_metadata_to_bins}
\title{Expand hexbin results by metadata groups}
\usage{
.add_metadata_to_bins(
  obj,
  cID,
  hexbin_matrix,
  bin_ids,
  metadata_var,
  dr_name,
  use_dims
)
}
\arguments{
\item{obj}{A SCUBA-supported single-cell object}

\item{cID}{Vector of cell-to-bin assignments from hexbin}

\item{hexbin_matrix}{Matrix with x, y coordinates and total cell counts}

\item{bin_ids}{Vector of unique bin IDs from hexbin object (used to map bin IDs to matrix rows)}

\item{metadata_var}{String naming the metadata variable}

\item{dr_name}{Name of dimension reduction (for cell ordering)}

\item{use_dims}{Dimensions used (for cell ordering)}
}
\value{
Data frame with columns: x, y, number_of_cells, and metadata_var
}
\description{
Creates an expanded data frame where each bin appears once per metadata
group, with cell counts calculated separately for each group. Bins with
zero cells for a particular group are excluded from that group's facet.
Cells with NA metadata are treated as a separate "NA" group.
}
\keyword{internal}
