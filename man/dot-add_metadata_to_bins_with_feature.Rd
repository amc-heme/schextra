% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schextra_utils.R
\name{.add_metadata_to_bins_with_feature}
\alias{.add_metadata_to_bins_with_feature}
\title{Expand hexbin results by metadata groups with feature expression}
\usage{
.add_metadata_to_bins_with_feature(
  obj,
  cID,
  hexbin_matrix,
  bin_ids,
  feature_values,
  action,
  metadata_var,
  dr_name,
  use_dims
)
}
\arguments{
\item{obj}{A SCUBA-supported single-cell object}

\item{cID}{Vector of cell-to-bin assignments from hexbin}

\item{hexbin_matrix}{Matrix with x, y coordinates and total cell counts}

\item{bin_ids}{Vector of unique bin IDs from hexbin object}

\item{feature_values}{Numeric vector of expression values (one per cell)}

\item{action}{Aggregation method: "mean", "median", or "sum"}

\item{metadata_var}{String naming the metadata variable}

\item{dr_name}{Name of dimension reduction (for cell ordering)}

\item{use_dims}{Dimensions used (for cell ordering)}
}
\value{
Data frame with columns: x, y, feature_value, and metadata_var
}
\description{
Creates an expanded data frame where each bin appears once per metadata
group, with feature expression aggregated separately for each group.
Bins with zero cells for a particular group are excluded from that
group's facet.
}
\keyword{internal}
